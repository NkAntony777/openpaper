Dense passage retrieval encodes queries and documents independently {cite_001}.
The dual-encoder design lets the system precompute document vectors and retrieve
nearest neighbours at inference time without cross-attention over the full corpus.
This introduction states no causal social claims and uses only ledger citations.
